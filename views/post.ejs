<%- include('partials/header') %>
    <div class="container mt-5">
        <div class="card shadow-lg post-detail">
            <div class="card-body">
                <h1 class="card-title mb-4"><%= post.title %></h1>
                 <div class="mb-4">
                    <%= post.content %>
                  </div>
                  <div class="text-end">
                     <a href="/" class="btn btn-secondary btn-sm"> <i class="fas fa-arrow-left me-1"></i> Back to Home</a>
                  </div>
            </div>
        </div>
    </div>
<%- include('partials/footer') %>